<nav class="w-full px-6 py-4 relative z-50">
  <div class="max-w-7xl mx-auto flex items-center justify-between relative">
    <!-- Logo -->
    <a href="#" class="font-bold text-xl text-gradient text-gradient-hover">
      Kath.<span class="font-light">dev</span>
    </a>

    <!-- Menú centrado en desktop -->
    <ul class="hidden md:flex gap-6 absolute left-1/2 transform -translate-x-1/2 text-gradient text-gradient-hover font-semibold">
      <li><a href="#about-me" class="hover:text-purple-500 text-xl">Sobre mí</a></li>
      <li><a href="#projects" class="hover:text-purple-500 text-xl">Proyectos</a></li>
      <li><a href="#contact" class="hover:text-purple-500 text-xl">Contacto</a></li>
    </ul>

    <!-- Interruptor modo oscuro (derecha en desktop) -->
    <div class="hidden md:block ml-auto">
      <label class="relative inline-block w-12 h-6 cursor-pointer">
        <input type="checkbox" id="dark-mode-toggle" class="sr-only" />
        <span class="slider absolute inset-0 bg-gradient-kathy rounded-full flex items-center justify-between px-1 text-xs text-white transition-all shadow-md">
          <i class="fa-solid fa-sun icon-sun"></i>
          <i class="fa-solid fa-moon icon-moon"></i>
          <span class="dot absolute top-[2px] left-[2px] h-5 w-5 bg-white rounded-full transition-transform"></span>
        </span>
      </label>
    </div>

    <!-- Botón hamburguesa (solo mobile) -->
    <button
      id="nav-toggle"
      class="md:hidden flex flex-col justify-center items-center gap-1"
      aria-label="Abrir menú"
      aria-expanded="false"
    >
      <span class="w-6 h-0.5 bg-gradient-kathy rounded"></span>
      <span class="w-6 h-0.5 bg-gradient-kathy rounded"></span>
      <span class="w-6 h-0.5 bg-gradient-kathy rounded"></span>
    </button>
  </div>

  <!-- Menú mobile colapsado -->
  <div id="mobile-menu" class="md:hidden mt-4 hidden">
    <ul class="flex flex-col items-center gap-4 text-gradient text-gradient-hover font-semibold">
      <li><a href="#about-me" class="hover:text-purple-400">Sobre mí</a></li>
      <li><a href="#projects" class="hover:text-purple-400">Proyectos</a></li>
      <li><a href="#contact" class="hover:text-purple-400">Contacto</a></li>
      <!-- Switch dark mode (mobile) -->
      <li class="flex justify-center">
        <label class="relative inline-block w-10 h-5 cursor-pointer">
          <input type="checkbox" id="dark-mode-toggle-mobile" class="sr-only" />
          <span class="slider absolute inset-0 bg-gradient-kathy rounded-full flex items-center justify-between px-1 text-xs text-white transition-all shadow-md">
            <i class="fa-solid fa-sun icon-sun"></i>
            <i class="fa-solid fa-moon icon-moon"></i>
            <span class="dot absolute top-[2px] left-[2px] h-4 w-4 bg-white rounded-full transition-transform"></span>
          </span>
        </label>
      </li>
    </ul>
  </div>
</nav>


<style>
  /* -------------------- SWITCH MODO OSCURO -------------------- */
  .slider .dot {
    transition: transform 0.3s ease;
  }

  #dark-mode-toggle:checked + .slider .dot,
  #dark-mode-toggle-mobile:checked + .slider .dot {
    transform: translateX(100%);
  }
</style>
